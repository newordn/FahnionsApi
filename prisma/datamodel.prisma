type Tshirt{
    id: ID! @id
    title: String!
    description: String!
    price: String!
    createdAt: DateTime! @createdAt
    images: [String!]! @scalarList(strategy: RELATION)
    contact: String!
}